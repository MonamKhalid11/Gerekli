```js

      <View style={styles.container}>
        <Button
          type="primary"
          onPress={() => onBtnPress()}
          disabled={disabled}
        >
          <Text style={styles.placeOrderBtnText}>
            {btnText}
          </Text>
        </Button>
      </View>

```